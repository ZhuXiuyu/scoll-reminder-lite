# 🚀 Scroll Reminder v1.1 (Lite / 开源版) - 最终研发 PRD

## 1. 产品概述
一款极简、严格的防沉迷浏览器扩展。针对高危内容平台，采用**“默认全局监控 + 核心功能区白名单放行”**的策略。辅以“自定义摸鱼网站”功能、防抖滚动计数、缓慢衰减及阶梯式强打断机制，帮助用户在无意识滑动时找回理智。

---

## 2. 核心架构与存储
- **权限需求**：`storage` (用于保存配置), `activeTab` 或 `<all_urls>` (用于注入脚本)。
- **存储结构 (chrome.storage.local)**：
  ```json
  {
    "enabledSites": {
      "zhihu": true,
      "bilibili": true,
      "xiaohongshu": true,
      "weibo": true,
      "douyin": true,
      "youtube": true
    },
    "customSites": ["v2ex.com", "hupu.com"] // 用户自定义的全局监控域名
  }
  ```

---

## 3. URL 匹配与运行策略 (核心改动)

在 `content_script.js` 加载时，首先读取本地存储的配置，并根据当前 `window.location.hostname` 和 `window.location.pathname` 决定是否启动滚动监听。

### 3.1 默认高危网站路由策略 (混合判定)

| 网站/标识 | 匹配 Hostname (包含) | 判定模式 | 路由规则说明 (正则或字符串前缀) |
| :--- | :--- | :--- | :--- |
| **知乎** | `zhihu.com` | **白名单放行** | 默认全部监控。<br>**放行 (不监控)**: `/search`, `/people`, `/collection`, `/education`, `/consult` |
| **Bilibili** | `bilibili.com` | **白名单放行** | 默认全部监控。<br>**放行 (不监控)**: `/video/`, `/search`, `space.bilibili.com` (个人空间), `/fav` |
| **小红书** | `xiaohongshu.com` | **白名单放行** | 默认全部监控。<br>**放行 (不监控)**: `/user/profile`, `/search/` |
| **微博** | `weibo.com` | **白名单放行** | 默认全部监控。<br>**放行 (不监控)**: `/u/` (个人主页), `/search` |
| **抖音/TikTok** | `douyin.com`, `tiktok.com` | **全局监控** | 全部监控，无白名单。 |
| **YouTube** | `youtube.com` | **黑名单监控** | **仅监控**: `/shorts`。<br>其余一律不监控 (首页和视频页放行)。 |

### 3.2 自定义网站策略 (Guilty Pleasure)
- 如果当前域名的 hostname 包含在用户的 `customSites` 列表中，则**完全忽略 Path，执行全局监控**。

---

## 4. Popup 交互界面设计

点击浏览器扩展图标，弹出极简设置面板。UI 风格要求：干净、极简、暗黑/亮色自适应或纯白底色。

```text
┌─────────────────────────────────┐
│  🛑 Scroll Reminder (Lite)      │
├─────────────────────────────────┤
│                                 │
│  [ 插件总开关 ]                  │
│  插件已启用 [●───────○]          │
│                                 │
├─────────────────────────────────┤
│                                 │
│  [ 默认监控站点 ]                │
│  ☑ 小红书 (xiaohongshu.com)     │
│  ☑ 哔哩哔哩 (bilibili.com)      │
│  ☑ 知乎 (zhihu.com)             │
│  ☑ 微博 (weibo.com)             │
│  ☑ 抖音/TikTok                  │
│  ☑ YouTube (仅 Shorts)          │
│                                 │
│  [ 自定义高危网站 (全局监控) ]    │
│  +--------------------+-------+ │
│  | 输入域名(如v2ex.com)  | 添加  | │
│  +--------------------+-------+ │
│                                 │
│  • v2ex.com               [删]  │
│  • tieba.baidu.com        [删]  │
│                                 │
│  * 修改后立即生效                │
└─────────────────────────────────┘
```

**插件禁用状态：**
```text
┌─────────────────────────────────┐
│  🛑 Scroll Reminder (Lite)      │
├─────────────────────────────────┤
│                                 │
│  [ 插件总开关 ]                  │
│  插件已禁用 [○───────●]          │
│                                 │
├─────────────────────────────────┤
│  [ 默认监控站点 ]                │
│  (变灰不可操作)                  │
│                                 │
│  插件已禁用，开启后可配置站点     │
│                                 │
└─────────────────────────────────┘
```
**交互逻辑：**
- 主开关控制插件全局启用/禁用，禁用后站点列表变灰不可操作
- 勾选/取消勾选默认站点，立刻保存到 `storage` 并实时通知已打开的标签页
- 输入自定义域名，点击添加，渲染到下方列表并保存。点击 [删] 移除
- 所有配置变更实时生效，无需刷新页面

---

## 5. 核心统计算法
- **触发源**：`wheel`, `touchmove`。
- **防抖 (Debounce)**：**600ms** (手指/滚轮停下 0.6 秒算作一次连续动作结束)。
- **时间衰减**：`Math.floor(距离上次滚动的秒数 / 60)`。每经过 60 秒，Count 自动减 1。
- **豁免机制**：若用户点击了弹窗中的延时按钮，设置 `isSuspended = true` 和 `suspendEndTime`，在此时间前跳过所有计数逻辑。

---

## 6. 渐进式干预 UI/UX 规范 (沿用，保持不变)

UI 必须由 JS 动态创建 DOM 插入 `document.body`，确保最高层级 `z-index: 2147483647`。

### 🚨 Level 1: 呼吸警戒线 (Count == 3)
- **触发**：拦截滚动计数。
- **UI**：屏幕四周全屏内发光 `box-shadow: inset 0 0 60px rgba(255, 102, 0, 0.6);`，包含呼吸动画（Opacity 渐变），`pointer-events: none`。
- **周期**：显示 4 秒后渐隐消失。随后进入 5 秒冷却期。

### 🚨 Level 2: 灵魂拷问软提醒 (Count == 6)
- **触发**：拦截滚动计数。
- **UI**：从屏幕正上方（偏下 40px）平滑下拉 Toast 气泡。
- **文案**：`"👀 嘿，你是不是又开始漫无目的地刷了？"`
- **样式**：黑底白字，毛玻璃背景，圆角 12px。
- **周期**：悬浮 4 秒后上滑消失。随后进入 10 秒冷却期。

### 🚨 Level 3: 强制物理隔离 (Count == 9)
- **触发**：拦截所有交互，锁定页面滚动 (`overflow: hidden`)。
- **UI**：全屏高强度毛玻璃遮罩 (`backdrop-filter: blur(15px); background: rgba(0,0,0,0.5);`)。**无右上角关闭按钮**。
- **弹窗选项**：
  1. `[ 给我 3 分钟收尾 ]`：解除遮罩，重置计数，暂停监听 3 分钟。
  2. `[ 查资料模式 ]` + 输入框 (默认 15) + "分钟"：解除遮罩，重置计数，暂停监听 X 分钟。
  3. `[ 离开此页 (推荐) ]`：高亮红色。执行 `window.location.href = 'about:blank'`。

---
## 7. 需要砍掉和不做的内容

### 7.1 需要**砍掉**的功能

- 白名单
- 数据统计
    - 每日触发提醒次数
    - 选择"离开" vs "继续"的次数
    - 各网站的触发频率
    - 连续成功离开的天数（streak）

### 7.2 不做的功能
本版本**不做**以下内容：

- 不识别“沉迷看垃圾内容”的语义场景
- 不做页面表面识别（如评论区、推荐流 DOM 识别）
- 不做复杂评分机制
- 不做多标签页共享状态
- 不做移动端适配

## 8. 交付要求 (给 AI 的指令)
- 请提供 `manifest.json`, `popup.html`, `popup.js`, `content_script.js` 和 CSS 样式（可写在 JS 内联或单独的 CSS 文件中）。
- 请使用原生 JavaScript (Vanilla JS)，不依赖任何外部框架如 React/Vue 或 jQuery。
- 确保 `content_script.js` 在加载时异步读取 `chrome.storage.local`，并严格执行 Section 3 的路由判定逻辑。
- 针对 Level 3 弹窗，需要锁定原网页的 `document.body.style.overflow`，并在点击继续浏览时恢复。